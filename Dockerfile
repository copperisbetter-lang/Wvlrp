FROM bluenviron/mediamtx:1 AS mediamtxFROM alpine:3.22# build-refresh: 2026-09-21 watchdog-v2RUN apk add --no-cache nginx ffmpeg python3COPY --from=mediamtx /mediamtx /mediamtxCOPY relay/mediamtx.yml /mediamtx.ymlCOPY relay/viewer_count.py /viewer_count.pyCOPY relay/roost_watchdog.py /roost_watchdog.pyRUN mkdir -p /run/nginx && printf '%s\n' \'worker_processes 1;' \'events { worker_connections 1024; }' \'http {' \'  access_log /dev/stdout;' \'  error_log /dev/stderr info;' \'  server {' \'    listen 8888;' \'    server_name _;' \'    location = / {' \'      default_type text/plain;' \'      return 200 "WVLRP multi-camera relay\\n";' \'    }' \'    location /presence/ {' \'      proxy_pass http://127.0.0.1:8894;' \'      proxy_http_version 1.1;' \'      proxy_set_header Host $host;' \'      proxy_set_header X-Real-IP $remote_addr;' \'      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;' \'      proxy_set_header X-Forwarded-Proto $scheme;' \'      proxy_buffering off;' \'    }' \'    location /webrtc/ {' \'      rewrite ^/webrtc/(.*)$ /$1 break;' \'      proxy_pass http://127.0.0.1:8889;' \'      proxy_http_version 1.1;' \'      proxy_set_header Host $host;' \'      proxy_set_header X-Forwarded-Proto $scheme;' \'      proxy_buffering off;' \'      proxy_request_buffering off;' \'      proxy_read_timeout 3600s;' \'    }' \'    location / {' \'      proxy_pass http://127.0.0.1:8891;' \'      proxy_http_version 1.1;' \'      proxy_set_header Host $host;' \'      proxy_set_header X-Real-IP $remote_addr;' \'      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;' \'      proxy_set_header X-Forwarded-Proto $scheme;' \'      proxy_buffering off;' \'      proxy_request_buffering off;' \'      proxy_read_timeout 3600s;' \'      proxy_send_timeout 3600s;' \'    }' \'  }' \'}' > /etc/nginx/nginx.confEXPOSE 8888
